const stats = {
    totalDonors: 150,
    totalRequests: 75
  };
  
  document.getElementById('total-donors').innerText = stats.totalDonors;
  document.getElementById('total-requests').innerText = stats.totalRequests;